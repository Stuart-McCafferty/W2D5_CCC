listID = []

class Guest:
    def __init__(self, _name, _wallet):
        self.name = _name
        self.wallet = _wallet
        
    
    # def create_guest(self):
    #     guestID = input("Enter guests ID: ")
    #     guestName = input("Enter name of customer: ")
    #     guestWallet = input("Enter guests wallet: ")
    #     listID.append(guestID)
    #     self.listId[0] = Guest(guestID, guestName, guestWallet)
    #       MAYBE NEED WHILE LOOP... not sure how to create during runtime without
    #       rewriting over the same object then adding to a list
    # create_guest(self)

